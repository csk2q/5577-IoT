# Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# Database Configuration
DB_ROOT_PASSWORD=rootpassword
DB_NAME=nurse_station_db
DB_USER=nurse_station_user
DB_PASSWORD=securepassword

# Backend Configuration
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-minimum-32-characters
JWT_EXPIRES_IN=8h

# Frontend Configuration (handled via docker-compose)
# VITE_API_BASE_URL is set in docker-compose.yml
